import type { Metadata } from "next";
export const metadata: Metadata = { title: "Global Search · DARKINT" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
