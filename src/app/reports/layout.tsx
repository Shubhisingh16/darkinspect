import type { Metadata } from "next";
export const metadata: Metadata = { title: "Intelligence Reports · DARKINT" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
