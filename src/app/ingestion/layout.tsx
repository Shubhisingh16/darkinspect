import type { Metadata } from "next";
export const metadata: Metadata = { title: "Ingestion Pipeline · DARKINT" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
